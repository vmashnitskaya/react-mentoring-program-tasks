interface MoreData {
    title: string;
    value: string;
}

const moreData: Array<MoreData> = [
    { title: 'Edit', value: 'edit' },
    { title: 'Delete', value: 'delete' },
];

export default moreData;
